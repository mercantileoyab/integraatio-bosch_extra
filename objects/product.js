class Product{
    constructor(
        articleNr,
        rule,
        parameter
    ) {
        this.articleNr = articleNr,
        this.rule = rule,
        this.parameter = parameter
    }
}

module.exports = Product